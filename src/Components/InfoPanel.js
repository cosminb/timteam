import React from 'react';
import _ from 'lodash';
import { DataCtx } from '../FlowGrid/DataCtx';

export const InfoPanel = ({ style }) => {
  return (
    <div style={style} className="infopanel">
      INFO panel
    </div>
  );
};
